// Operader de String (String operator)

// Comparação (comparison)
// console.log('a' == 'a')

// Contatenação (concatenation)
// Retorna a união de duas Strings
let alpha = 'alpha'
alpha += 'bet'
console.log(alpha)