/*
  ## Operaderes de comparação

    ** Irá comparar valores e retornar um Boolean como resposta à comparação
*/

// --> apenas 1 = é sinal de atribuição
let one = 1
let two = 2

// == igual
    // console.log(one == 1)
    // console.log(one == '1')

// != diferente
    // console.log(one != two)
    // console.log(one != 1)
    // console.log(one != "1")

// === estritamente igual
        // console.log(one === "1")
        // console.log(one === 1)

// !== estritamente diferente
        // console.log(one !== "1")
        // console.log(one !== 1)
        // console.log(one !== two)

// > maior
    // console.log(one > two)
    // console.log(one > 1)
    // console.log(two > 1)

// >= maior igual
      // console.log(one >= two)
      // console.log(one >= 1)
      // console.log(one >= 0)

// < menor
    // console.log(one < two)
    // console.log(one < 1)
    // console.log(two < 1)

// <= menor igual
      // console.log(one <= two)
      // console.log(one <= 1)
      // console.log(one <= 0)
      